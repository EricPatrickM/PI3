/*export default class Table extends Phaser.Scene{

    constructor(config){
        super(config);
    }

    preLoad(){
        this.load.image('table','/assets/table.jpg');
    }
    create(){
        this.add.image(200, 100, 'table');
    }
    update(){
        
    }
}
*/